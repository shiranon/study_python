# 引数nが素数かどうか調べる関数(未)
def is_prime(n):
    # TODO: ここに実装
    return False


# 関数is_primeをテストする
def test_is_prime():
    assert is_prime(3)
    assert not is_prime(8)
    assert is_prime(89)
